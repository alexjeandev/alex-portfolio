function Footer() {
  return (
    <footer className="footer">
      <p>© {new Date().getFullYear()} Alex Jean — Designed & built with React + TypeScript.</p>
    </footer>
  )
}

export default Footer